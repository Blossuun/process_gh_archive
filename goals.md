# 프로젝트 목표

GH Archive 공개 이벤트 로그를 처리하는 데이터 파이프라인을 만든다.

**이 프로젝트의 산출물은 파이프라인이다.** 분석 결과는 파이프라인이 쓸 만한 데이터를 만들어낸다는 증명이며, 그 자체가 목적은 아니다.

---

## 왜 이 구조인가

`data_analysis/BQ_EDA/analysis/findings.md` 의 조사 결과, 이 데이터로 분석적 결론을 내기에는 제약이 크다.

- 2025-10 부터 payload 필드 대부분이 제거됐다 (GitHub Events API 정책 변경, 2025-08-08 체인지로그)
- 2025년 중반부터 PushEvent 외 이벤트가 심하게 과소 집계된다 (ClickHouse 의 GitHub 데이터셋도 같은 진단)
- 2025-10-08~14 에 GitHub Event API 장애로 약 20% 가 영구 결손됐다 (GitHub 커뮤니티 #178788)

**그런데 같은 제약이 데이터 엔지니어링 재료로는 좋다.** 실제 breaking change, 실제 품질 결손, 실제 규모가 모두 갖춰져 있다. 대부분의 포트폴리오 프로젝트는 이런 상황을 시나리오로 지어내야 한다.

---

## 주 목표 — 원본을 평탄화한 silver 테이블 파이프라인

중첩 JSON 이벤트 로그를 질의 가능한 정형 테이블로 변환하는 배치 파이프라인을 만든다. 다음 5개 축으로 역량을 보인다.

### 축 1 — 규모

수년치 원본을 실제로 처리한다.

| 지표 | 목표 |
|---|---|
| 처리 기간 | 미확정 (원본 용량 실측 후 결정) |
| 이벤트 건수 | 억 단위 |
| 입력 용량 | 미확정 |
| 처리량 | 건/초, GB/분 측정 |
| 총 소요 시간 | 전체 백필 기준 측정 |

숫자를 남기는 것이 목적이다. "처리했다" 가 아니라 "N억 건을 X시간에 처리했다" 로 서술한다.

### 축 2 — 스키마 진화 감지와 대응

변경을 처리하는 것이 아니라 **변경을 감지하는 장치**를 만든다. 특정 날짜를 아는 채로
분기를 넣으면 다음 변경에는 무력하다.

- 키 집합 감시로 새 키·사라진 키를 탐지 (`DiscussionEvent` 신규 타입 포함)
- null 비율 감시로 컬럼이 남은 채 값만 사라지는 경우를 탐지 (`pull_request.title` 이 이 경우)
- 감지 후 대응 규칙 추가. 라벨·담당자 이동은 두 경로를 순차 탐색하는 방식으로 처리
- **변환 코드에 날짜 리터럴을 넣지 않는다.** 하드코딩과 일반 규칙을 가르는 기준이다

**증명 방법:** 감지기를 먼저 만들고 대응 규칙 없이 전 기간을 처리해 경보 로그를 남긴다.
그 로그를 근거로 대응 규칙을 추가한다. git 히스토리가 순서를 증명한다.

### 축 3 — 품질 게이팅

부실한 데이터를 자동으로 격리한다. 실제 결손 사례가 데이터에 이미 있으므로
가짜 결손을 만들어 넣지 않는다.

- 3단계 검증: 시간별 파일 24개 존재 → 이벤트 수가 이동 중앙값 대비 정상 범위 → 시간대 분포 형태 정상
- 세 번째가 핵심이다. 총량이 정상이어도 시간대 편차가 사라지면 데이터 성격이 바뀐 것이다. 정상일은 최저/최고 시간 비율이 0.55~0.58, 이상일은 0.9 이상
- 판정 결과를 품질 메타데이터 테이블로 남기고, 분석 시 조인해 신뢰도를 표시한다
- **결측값을 보간하지 않는다.** 값을 지어내는 대신 불확실성을 드러낸다

**증명 방법:** 2025-10-09~14 같은 실제 결손 구간을 탐지·격리한 기록.

### 축 4 — 멱등성과 재처리

같은 날짜를 여러 번 처리해도 결과가 동일하다.

- `event.id` 기준 중복 제거
- 특정 날짜 재처리 시 기존 파티션만 교체
- 처리 이력을 로그 테이블로 관리해 중단 지점부터 재개 가능

**증명 방법:** 임의 날짜를 3회 재처리해 행 수와 체크섬이 불변임을 보인다.
프로세스 장애는 데이터가 제공하지 못하므로 주입해서 검증한다. 배치 중간 강제 종료,
손상 파일 투입, 미지 타입 투입. 주입한 실패는 사고가 아니라 테스트로 서술한다.

### 축 5 — 성능 튜닝

설계 선택에 근거를 남긴다.

- 파티션 전략 (날짜 단위 vs 월 단위) 비교
- 파일 크기 튜닝 (small file 문제)
- 압축 포맷·코덱 비교
- 병렬도와 메모리 설정

**증명 방법:** 튜닝 전후 수치와 판단 근거를 문서로 남긴다.

---

## 부 목표 — 파이프라인이 쓸 만한 결과를 낸다는 증명

분석 항목은 인사이트가 아니라 **기술 부하의 성격**으로 골랐다.

### A. 브랜치 명명 분포

`CreateEvent.ref` 를 전 기간에 걸쳐 파싱한다. 접두사(`feature/`, `fix/`, `dependabot/`), 구분자, 이슈 번호 포함 여부.

**거는 부하:** 수억 건 문자열 파싱과 대규모 카디널리티 집계
**데이터 상태:** payload 축소의 영향이 없어 전 기간 온전

### B. 저장소별 일별 스타 집계와 4주 롤링 baseline

`WatchEvent` 를 저장소·날짜로 집계하고 4주 이동 평균 대비 비율을 계산한다.

**거는 부하:** 대규모 윈도 함수와 시계열 자기 조인
**데이터 상태:** 2025년 중반 이후 과소 집계. 산출물에 신뢰 구간을 명시한다

두 워크로드는 성격이 달라서 성능 튜닝 사례를 두 종류 보여줄 수 있다.

---

## 범위 밖

이전 목표에서 제외한 것들과 이유.

| 항목 | 제외 이유 |
|---|---|
| 커밋 메시지 관례 분석 | 2025-10 부터 `commits` 배열이 사라져 분석 창이 갈라진다 |
| PR 크기 분석 | 같은 이유 |
| 급증 저장소 생존율 결론 | GitHub API 코호트 보강까지 가면 범위가 커지는데 얻는 것이 분석 결론 하나뿐이다 |
| GitHub API 로 스타 타임라인 검증 | 2026-07 부터 stargazers 목록 엔드포인트가 관리자·협업자로 제한됐다 (2026-06-30 체인지로그) |
| "교과서 대 실제" 프레이밍 | 데이터로 뒷받침하기 어렵고 주관이 개입한다 |

---

## 미확정

1. 처리 기간 — 원본 `.json.gz` 용량 실측 후 결정
2. 저장 포맷 — Parquet + Hive 파티션 vs Iceberg
3. silver 테이블 분리 단위
4. 부 목표 B 의 baseline 계산에서 이상일을 제외하는 규칙
5. 원본 보관 정책 — 처리 후 폐기 여부